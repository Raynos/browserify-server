{
  "name": "{{example}}",
  "version": "0.1.0",
  "description": "{{example}}",
  "author": "{{author}}",
  "main": "index",
  "dependencies": {
  },
  "devDependencies": {
    "browserify-server": "~2.1.2",
    "nodemon": "~0.6.23",
    "live-reload": "~0.2.0"
  },
  "scripts": {
    "build": "make all"
  }
}
